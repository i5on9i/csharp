position limit 에 도달해도 stop loss 에 다다르지 않았기 때문에 계속해서 현재값을 변화시켜줘야 한다.


매 tick 마다 값이 변한다.
그래서 매 순간에 따라서 Stop loss 에 다다를 수 있다.
stop loss 를 넘는 순간이 생기면,
trade 를 정리하고(현재가격으로 종료),
stop trading 을 한다.

tickReader 가 1초 이상을 붙잡고 있지 못하도록 할 수 있나?

---
06-26

average mid price 에서 current tick 의 mid 까지 포함한 500개인가요?
아니면 제외한 500개인가요?
현재는 current tick 을 포함한 값이다.



offset 을 낮추고,
tickcount 를 낮춰서, 
loss 가 meet 하도록 test

---
거래 한번에 가능한 최대 postion 값인가?


Position limit
남아있는 position limit 보다 trade size 가 큰 경우는 남아있는 만큼 할 것인가? 아니면 trade 하지 않을 것인가?

----
stop trade 라는 것을 하면
읽는 것을 멈추고, 매 tick 에 대한 총 손실 계산을 멈춘다.
